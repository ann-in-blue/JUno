package model.cards;

import model.Card;
import model.Color;

public class Draw2Card extends Card
{
	public Draw2Card(Color color, String nameImage)
	{
		setColor(color);
		setImage(nameImage);

	}

}
